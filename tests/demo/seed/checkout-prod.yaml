apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout
  namespace: demo-prod
  labels:
    app: checkout
    env: prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: checkout
  template:
    metadata:
      labels:
        app: checkout
        env: prod
    spec:
      containers:
        - name: checkout
          image: nginx:1.27
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
